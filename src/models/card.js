export default class Card {
    constructor(id, word, on, kun, description, image,meaning,isSaved){
        this.id = id
        this.word = word
        this.on = on
        this.kun = kun
        this.description = description
        this.image = image
        this.meaning = meaning
        this.isSaved = isSaved
    }
}